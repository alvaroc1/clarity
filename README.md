Work-in-progress electron app that listens for guacamole drawing commands on port 9002 and renders them on an HTML canvas.
Should be suitable as a remote-canvas or animation surface.

```
$ yarn run main
```
